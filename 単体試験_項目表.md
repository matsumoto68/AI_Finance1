# 単体試験 項目表

## CommandA.sh 単体試験

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 単体試験 項目表 |
| 対象シェル | `CommandA.sh` |
| 参照資料 | 外部設計書.md、内部設計書.md、エラーコード定義書.md |

---

## 2. 試験環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 接続方法 | Teraterm（SSH接続） |
| 実行ユーザー | 一般ユーザー（`$` プロンプト） |
| 前提 | `deploy_CommandA.sh` による資材配置完了後に実施 |

---

## 3. 試験項目一覧

### 凡例

| 記号 | 意味 |
|------|------|
| ○ | 正常終了（期待値と一致） |
| × | 異常終了（期待値と不一致） |
| - | 対象外 |

---

### 3.1 事前チェック（check_env）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| E-01 | ログディレクトリ作成確認 | `bash CommandA.sh` 実行後 `/var/log/release/` 存在確認 | 0 | `/var/log/release/` が存在する | | | | |
| E-02 | systemctl コマンド存在確認 | `command -v systemctl` | 0 | パス表示あり | | | | |
| E-03 | ログファイル生成確認 | `ls /var/log/release/CommandA_*.log` | 0 | ファイルが1件以上存在する | | | | |
| E-04 | ログヘッダー出力確認 | `head -4 /var/log/release/CommandA_*.log` | 0 | `スクリプト名: CommandA.sh` が含まれる | | | | |

---

### 3.2 ディレクトリ操作（proc_dir）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| D-01 | ディレクトリ作成（正常） | `CommandA.sh` 内 `mkdir /tmp/testdir` | 0 | `/tmp/testdir` が作成される | | | | |
| D-02 | ディレクトリ作成（既存・エラー確認） | 既存ディレクトリに対して `mkdir /tmp/testdir` | 1 | `File exists` が出力されシェルが続行する | | | | |
| D-03 | ディレクトリ作成（権限なし・エラー確認） | `mkdir /root/noperm` | 1 | `Permission denied` が出力されシェルが続行する | | | | |
| D-04 | ディレクトリ移動（正常） | `cd /tmp/testdir` | 0 | カレントディレクトリが `/tmp/testdir` になる | | | | |
| D-05 | ディレクトリ移動（不存在・エラー確認） | `cd /tmp/notexist` | 1 | `No such file or directory` が出力されシェルが続行する | | | | |
| D-06 | ディレクトリコピー | `cp -r /tmp/testdir /tmp/testdir_bk` | 0 | `/tmp/testdir_bk` が作成される | | | | |
| D-07 | ディレクトリ削除（空） | `rmdir /tmp/testdir_bk` | 0 | `/tmp/testdir_bk` が削除される | | | | |
| D-08 | ディレクトリ削除（非空・エラー確認） | `rmdir /tmp/testdir`（ファイルあり） | 1 | `Directory not empty` が出力されシェルが続行する | | | | |
| D-09 | ディレクトリ強制削除 | `rm -rf /tmp/testdir` | 0 | `/tmp/testdir` が削除される | | | | |

---

### 3.3 ファイル操作（proc_file）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| F-01 | ファイル作成（touch） | `touch /tmp/testdir/test.txt` | 0 | `/tmp/testdir/test.txt` が作成される | | | | |
| F-02 | ファイル作成（echo） | `echo hello > /tmp/testdir/data.txt` | 0 | `/tmp/testdir/data.txt` が作成され `hello` が書き込まれる | | | | |
| F-03 | ファイル内容確認 | `cat /tmp/testdir/data.txt` | 0 | `hello` が出力される | | | | |
| F-04 | ファイルコピー | `cp /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | `/tmp/testdir/data_bk.txt` が作成される | | | | |
| F-05 | ファイルコピー（上書き） | `cp -f /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | `/tmp/testdir/data_bk.txt` が上書きされる | | | | |
| F-06 | ファイル移動（リネーム） | `mv /tmp/testdir/test.txt /tmp/testdir/renamed.txt` | 0 | `test.txt` が消え `renamed.txt` が作成される | | | | |
| F-07 | ファイル削除 | `rm /tmp/testdir/data_bk.txt` | 0 | `/tmp/testdir/data_bk.txt` が削除される | | | | |
| F-08 | ファイル削除（不存在・エラー確認） | `rm /tmp/testdir/notexist.txt` | 1 | `No such file or directory` が出力されシェルが続行する | | | | |

---

### 3.4 サービス操作（proc_service）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| S-01 | サービス起動 | `systemctl start nginx` | 0 | nginx が起動する | | | | |
| S-02 | サービス起動失敗確認（エラーなし確認） | `systemctl start nginx` 再実行 | 0 | `Failed` が出力されない | | | | |
| S-03 | 起動確認 | `systemctl is-active nginx` | 0 | `active` が出力される | | | | |
| S-04 | サービス停止 | `systemctl stop nginx` | 0 | nginx が停止する | | | | |
| S-05 | 停止確認 | `systemctl is-active nginx` | 3 | `inactive` が出力される | | | | |
| S-06 | サービス状態確認 | `systemctl status nginx` | - | `Active` が出力に含まれる | | | | |

---

### 3.5 一覧表示・権限変更（proc_misc）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| M-01 | 一覧表示 | `ls -la /tmp` | 0 | `total` が出力に含まれる | | | | |
| M-02 | ファイル権限変更 | `chmod 755 /tmp/testfile.sh` | 0 | `/tmp/testfile.sh` の権限が `755` になる | | | | |

---

### 3.6 正常終了確認

| No | 試験項目 | 確認方法 | 期待する動作 | 実際の動作 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------|------|
| Z-01 | シェル正常終了確認 | `echo $?`（CommandA.sh 実行後） | `0` が出力される | | | |
| Z-02 | シェル実行ログ出力確認 | `cat /var/log/release/CommandA_*.log` | `[INFO] CommandA.sh 処理正常終了` が記録されている | | | |
| Z-03 | 詳細ログ出力確認 | `cat /var/log/release/CommandA_detail_*.log` | 各コマンドの `[CMD]` と `[RC]` が記録されている | | | |

---

## 4. 証跡取得一覧

| 証跡種別 | ファイル名 | 取得方法 |
|---------|-----------|---------|
| Teratermログ | Teraterm操作ログ（自動付番） | Teraterm設定によりセッションログとして自動保存 |
| シェル実行ログ | `/var/log/release/CommandA_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
| 詳細ログ | `/var/log/release/CommandA_detail_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
