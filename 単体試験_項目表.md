# 単体試験 項目表

## CommandA.sh 単体試験

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 単体試験 項目表 |
| 対象シェル | `CommandA.sh` |
| 参照資料 | 外部設計書.md、内部設計書.md、エラーコード定義書.md |

---

## 2. 試験環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 接続方法 | Teraterm（SSH接続） |
| 実行ユーザー | 一般ユーザー（`$` プロンプト） |
| 前提 | `deploy_CommandA.sh` による資材配置完了後に実施 |

---

## 3. 試験項目一覧

### 凡例

| 記号 | 意味 |
|------|------|
| ○ | 正常終了（期待値と一致） |
| × | 異常終了（期待値と不一致） |
| - | 対象外 |

> **エラー確認ステップの読み方：**  
> 「エラー確認」と付く試験項目は、「意図的にエラーが発生する状況を作り、Linuxが指定のエラーメッセージを出力すること」と「シェルがそのメッセージを検出して異常終了せず次ステップへ続行すること」の両方を確認します。

---

### 3.1 事前チェック（init_log / check_env）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| E-01 | ログディレクトリ自動作成確認 | `bash CommandA.sh` 実行後 `ls -la /var/log/release/` | 0 | `/var/log/release/` が存在する | | | | 初回実行時にディレクトリが自動作成されること |
| E-02 | systemctl コマンド存在確認 | `command -v systemctl` | 0 | systemctl のフルパスが表示される | | | | |
| E-03 | シェル実行ログ生成確認 | `ls /var/log/release/CommandA_*.log` | 0 | ログファイルが1件生成されている | | | | |
| E-04 | 詳細ログ生成確認 | `ls /var/log/release/CommandA_detail_*.log` | 0 | 詳細ログファイルが1件生成されている | | | | |
| E-05 | ログファイル命名規則確認 | `ls /var/log/release/CommandA_*.log` | 0 | ファイル名が `CommandA_YYYYMMDDHHMMSS.log` 形式（14桁の日時）である | | | | |
| E-06 | ログヘッダー出力確認 | `head -5 /var/log/release/CommandA_*.log` | 0 | `スクリプト名: CommandA.sh` と実行開始日時が含まれる | | | | |

---

### 3.2 ディレクトリ操作（proc_dir）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| D-01 | ディレクトリ作成（正常） | CommandA.sh 内 `mkdir /tmp/testdir` 実行後 `ls -la /tmp/testdir` | 0 | `/tmp/testdir` が作成されている | | | | |
| D-02 | ディレクトリ作成（既存・エラー確認） | 既存の `/tmp/testdir` に対して `mkdir /tmp/testdir` を実行 | 1 | Linux から `mkdir: cannot create directory '/tmp/testdir': File exists` が出力される／シェルは異常終了せず次ステップへ続行する | | | | |
| D-03 | ディレクトリ作成（権限なし・エラー確認） | `mkdir /root/noperm` を実行 | 1 | Linux から `mkdir: cannot create directory '/root/noperm': Permission denied` が出力される／シェルは異常終了せず次ステップへ続行する | | | | |
| D-04 | ディレクトリ移動（正常） | `cd /tmp/testdir` 実行後 `pwd` | 0 | カレントディレクトリが `/tmp/testdir` になっている | | | | |
| D-05 | ディレクトリ移動（不存在・エラー確認） | `cd /tmp/notexist` を実行 | 1 | Linux から `bash: cd: /tmp/notexist: No such file or directory` が出力される／シェルは異常終了せず次ステップへ続行する | | | | |
| D-06 | ディレクトリコピー | `cp -r /tmp/testdir /tmp/testdir_bk` 実行後 `ls -la /tmp/testdir_bk` | 0 | `/tmp/testdir_bk` が作成されている | | | | |
| D-07 | ディレクトリ削除（空） | `rmdir /tmp/testdir_bk` 実行後 `ls /tmp/testdir_bk 2>&1` | 0 | `No such file or directory` が出力される（ディレクトリが削除されている） | | | | |
| D-08 | ディレクトリ削除（非空・エラー確認） | ファイルを含む `/tmp/testdir` に対して `rmdir /tmp/testdir` を実行 | 1 | Linux から `rmdir: failed to remove '/tmp/testdir': Directory not empty` が出力される／シェルは異常終了せず次ステップへ続行する | | | | |
| D-09 | ディレクトリ強制削除 | `rm -rf /tmp/testdir` 実行後 `ls /tmp/testdir 2>&1` | 0 | `No such file or directory` が出力される（ディレクトリが削除されている） | | | | |

---

### 3.3 ファイル操作（proc_file）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| F-00 | WORK_DIR 再作成確認 | proc_dir 後に `ls -la /tmp/testdir` | 0 | `/tmp/testdir` が `mkdir -p` により再作成されている | | | | proc_dir で削除後に proc_file 内で自動再作成される |
| F-01 | ファイル作成（touch） | `touch /tmp/testdir/test.txt` 実行後 `ls -la /tmp/testdir/test.txt` | 0 | `/tmp/testdir/test.txt` が作成されている | | | | |
| F-02 | ファイル作成（echo） | `echo hello > /tmp/testdir/data.txt` 実行後 `ls -la /tmp/testdir/data.txt` | 0 | `/tmp/testdir/data.txt` が作成されている | | | | |
| F-03 | ファイル内容確認 | `cat /tmp/testdir/data.txt` | 0 | `hello` が出力される | | | | |
| F-04 | ファイルコピー | `cp /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` 実行後 `ls -la /tmp/testdir/data_bk.txt` | 0 | `/tmp/testdir/data_bk.txt` が作成されている | | | | |
| F-05 | ファイルコピー（上書き） | `cp -f /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` 実行後 `diff /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | 差分なし（上書きコピーが成功している） | | | | |
| F-06 | ファイル移動（リネーム） | `mv /tmp/testdir/test.txt /tmp/testdir/renamed.txt` 実行後 確認 | 0 | `test.txt` が存在せず `renamed.txt` が存在する | | | | |
| F-07 | ファイル削除 | `rm /tmp/testdir/data_bk.txt` 実行後 `ls /tmp/testdir/data_bk.txt 2>&1` | 0 | `No such file or directory` が出力される（ファイルが削除されている） | | | | |
| F-08 | ファイル削除（不存在・エラー確認） | 存在しない `/tmp/testdir/notexist.txt` に対して `rm` を実行 | 1 | Linux から `rm: cannot remove '/tmp/testdir/notexist.txt': No such file or directory` が出力される／シェルは異常終了せず次ステップへ続行する | | | | |

---

### 3.4 サービス操作（proc_service）

> **S-05 RC=3 の根拠：**  
> `systemctl is-active` コマンドは サービスが `active` 以外の状態（`inactive` / `failed` 等）のとき RC=3 を返す（systemd 仕様）。  
> RC=1 はシェルスクリプト汎用エラーコードであり、`systemctl is-active` の停止状態を意味しない。

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| S-01 | サービス起動 | `systemctl start nginx` 実行後 `systemctl is-active nginx` | 0 | nginx が起動し `active` が出力される | | | | |
| S-02 | サービス起動失敗確認（Failedなし確認） | 起動済み nginx に対して再度 `systemctl start nginx` を実行 | 0 | Linux からの出力に `Failed` が含まれない／シェルは異常終了せず次ステップへ続行する | | | | 起動済みサービスへの再起動は通常 RC=0 で `Failed` なし |
| S-03 | 起動確認 | `systemctl is-active nginx` | 0 | `active` が1行で出力される | | | | |
| S-04 | サービス停止 | `systemctl stop nginx` 実行後 `systemctl is-active nginx` | 3 | nginx が停止し `inactive` が出力される | | | | |
| S-05 | 停止確認 | `systemctl is-active nginx`（停止後） | 3 | `inactive` が1行で出力される（RC=3 は systemd 仕様で停止状態を表す正常な終了コード） | | | | RC=1 ではなく RC=3 が停止を意味する（systemd 仕様） |
| S-06 | サービス状態確認 | `systemctl status nginx` | - | 出力に `Active:` の行が含まれる | | | | RC は状態により変わるため `-` とする |

---

### 3.5 一覧表示・権限変更（proc_misc）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| M-01 | 一覧表示 | `ls -la /tmp` | 0 | 出力の先頭に `total` で始まる行が含まれる | | | | |
| M-02 | ファイル権限変更 | `chmod 755 /tmp/testfile.sh` 実行後 `ls -la /tmp/testfile.sh` | 0 | パーミッション表示が `-rwxr-xr-x` になっている | | | | 実行前の権限が `644` であることを確認してから実施 |
| M-03 | 権限変更前後の差異確認 | 変更前後の `ls -la /tmp/testfile.sh` 出力を比較 | - | 変更前は `-rw-r--r--`（644）、変更後は `-rwxr-xr-x`（755）になっている | | | | |

---

### 3.6 エラー異常終了確認（エラーハンドリング）

| No | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 合否 | 備考 |
|----|---------|------------------|-----------|----------------|---------|-----------|------|------|
| A-01 | ログディレクトリ書き込み不可時の異常終了 | `/var/log/release` を `chmod 444` してから `bash CommandA.sh` | 1 | シェル実行ログに `[ERROR] E001:` が出力されRC=1で終了する | | | | 試験後に `chmod 755` で元に戻すこと |
| A-02 | systemctl 不存在時の異常終了 | `PATH` を `systemctl` を含まないパスに変更して `bash CommandA.sh` | 1 | シェル実行ログに `[ERROR] E002:` が出力されRC=1で終了する | | | | 試験後に PATH を元に戻すこと |

---

### 3.7 正常終了確認

| No | 試験項目 | 確認方法 | 期待する動作 | 実際の動作 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------|------|
| Z-01 | シェル正常終了確認 | `echo $?`（CommandA.sh 実行直後） | `0` が出力される | | | |
| Z-02 | 正常終了ログ確認 | `grep "処理正常終了" /var/log/release/CommandA_*.log` | `[INFO]  CommandA.sh 処理正常終了` が記録されている | | | |
| Z-03 | CMD ログ完全出力確認 | `grep "\[CMD\]" /var/log/release/CommandA_detail_*.log \| wc -l` | 25件以上の `[CMD]` ログが記録されている | | | |
| Z-04 | RC ログ完全出力確認 | `grep "\[RC\]" /var/log/release/CommandA_detail_*.log \| wc -l` | `[CMD]` と同数の `[RC]` ログが記録されている | | | |
| Z-05 | ERROR ログ未出力確認（正常時） | `grep "\[ERROR\]" /var/log/release/CommandA_*.log` | `[ERROR]` が1件も記録されていない | | | |

---

## 4. 証跡取得一覧

| 証跡種別 | ファイル名 | 取得方法 |
|---------|-----------|---------|
| Teratermログ | Teraterm操作ログ（自動付番） | Teraterm設定によりセッションログとして自動保存 |
| シェル実行ログ | `/var/log/release/CommandA_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
| 詳細ログ | `/var/log/release/CommandA_detail_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
