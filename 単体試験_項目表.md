# 単体試験 項目表

## CommandA.sh 単体試験

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 単体試験 項目表 |
| 対象シェル | `CommandA.sh` |
| 参照資料 | 外部設計書.md、内部設計書.md、エラーコード定義書.md |

---

## 2. 試験環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 接続方法 | Teraterm（SSH接続） |
| 実行ユーザー | 一般ユーザー（`$` プロンプト） |
| 前提 | `deploy_CommandA.sh` による資材配置完了後に実施 |

---

## 3. 試験項目一覧

### 凡例

| 記号 | 意味 |
|------|------|
| ○ | 正常終了（期待値と一致） |
| × | 異常終了（期待値と不一致） |
| - | 対象外 |

> **エラー確認ステップの読み方：**  
> 「エラー確認」と付く試験観点は、「意図的にエラーが発生する状況を作り、Linuxが指定のエラーメッセージを出力すること」と「シェルがそのメッセージを検出して異常終了せず次ステップへ続行すること」の両方を確認します。

---

### 3.1 事前チェック（init_log / check_env）

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| E-01 | ログディレクトリ自動作成 | `/var/log/release/` が存在しない場合に自動作成されること | `bash CommandA.sh` 実行後 `ls -la /var/log/release/` | 0 | `/var/log/release/` が存在する | 0 | `/var/log/release/` が存在することを確認 | 2026-09-08 | Kiro | - | ○ | 初回実行時に確認 |
| E-02 | systemctl コマンド存在確認 | `systemctl` コマンドが使用可能な環境であること | `command -v systemctl` | 0 | systemctl のフルパスが表示される | 0 | `[INFO] 事前チェック完了` がログに出力された | 2026-09-08 | Kiro | - | ○ | |
| E-03 | シェル実行ログ生成 | シェル実行後にシェル実行ログが1件生成されること | `ls /var/log/release/CommandA_*.log` | 0 | ログファイルが1件生成されている | 0 | `CommandA_20260908084224.log` が生成された | 2026-09-08 | Kiro | - | ○ | |
| E-04 | 詳細ログ生成 | シェル実行後に詳細ログが1件生成されること | `ls /var/log/release/CommandA_detail_*.log` | 0 | 詳細ログファイルが1件生成されている | 0 | `CommandA_detail_20260908084224.log` が生成された | 2026-09-08 | Kiro | - | ○ | |
| E-05 | ログファイル命名規則 | ログファイル名が `CommandA_YYYYMMDDHHMMSS.log` 形式（14桁日時）であること | `ls /var/log/release/CommandA_*.log` | 0 | ファイル名が `CommandA_YYYYMMDDHHMMSS.log` 形式（14桁の日時）である | 0 | `CommandA_20260908084224.log`（14桁）形式を確認 | 2026-09-08 | Kiro | - | ○ | |
| E-06 | ログヘッダー出力 | ログヘッダーにスクリプト名と実行開始日時が記録されること | `head -5 /var/log/release/CommandA_*.log` | 0 | `スクリプト名: CommandA.sh` と実行開始日時が含まれる | 0 | `スクリプト名: CommandA.sh` / `実行開始日時: 2026-09-08 08:42:24` を確認 | 2026-09-08 | Kiro | - | ○ | |

---

### 3.2 ディレクトリ操作（proc_dir）

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| D-01 | ディレクトリ作成（正常） | 指定パスにディレクトリが正常に作成されること | `mkdir /tmp/testdir` 実行後 `ls -la /tmp/testdir` | 0 | `/tmp/testdir` が作成されている | 0 | `[INFO] ディレクトリ作成完了: /tmp/testdir` を確認 | 2026-09-08 | Kiro | - | ○ | |
| D-02 | ディレクトリ作成（既存・エラー確認） | 既存ディレクトリへの作成時に `File exists` が出力され、シェルが異常終了しないこと | 既存の `/tmp/testdir` に対して `mkdir /tmp/testdir` を実行 | 1 | Linux から `mkdir: cannot create directory '/tmp/testdir': File exists` が出力される／シェルは異常終了せず次ステップへ続行する | 1 | RC=1、`File exists` 出力を確認。シェルは続行し `[INFO] ディレクトリ作成（既存）エラー確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | |
| D-03 | ディレクトリ作成（権限なし・エラー確認） | 権限のないパスへの作成時に `Permission denied` が出力され、シェルが異常終了しないこと | `mkdir /root/noperm` を実行 | 1 | Linux から `mkdir: cannot create directory '/root/noperm': Permission denied` が出力される／シェルは異常終了せず次ステップへ続行する | 1 | RC=1、`Permission denied` 出力を確認。シェルは続行し `[INFO] ディレクトリ作成（権限なし）エラー確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | ec2-userで実行したため正常にPermission deniedが発生した |
| D-04 | ディレクトリ移動（正常） | 指定パスへのカレントディレクトリ移動が正常に行われること | `cd /tmp/testdir` 実行後 `pwd` | 0 | カレントディレクトリが `/tmp/testdir` になっている | 0 | RC=0、`[INFO] ディレクトリ移動完了: /tmp/testdir` を確認 | 2026-09-08 | Kiro | - | ○ | |
| D-05 | ディレクトリ移動（不存在・エラー確認） | 存在しないパスへの移動時に `No such file or directory` が出力され、シェルが異常終了しないこと | `cd /tmp/notexist` を実行 | 1 | Linux から `bash: cd: /tmp/notexist: No such file or directory` が出力される／シェルは異常終了せず次ステップへ続行する | 1 | RC=1、`No such file or directory` 出力を確認。シェルは続行し `[INFO] ディレクトリ移動（不存在）エラー確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | |
| D-06 | ディレクトリコピー | ディレクトリがサブディレクトリ含め正常にコピーされること | `cp -r /tmp/testdir /tmp/testdir_bk` 実行後 `ls -la /tmp/testdir_bk` | 0 | `/tmp/testdir_bk` が作成されている | 0 | RC=0、`[INFO] ディレクトリコピー完了: /tmp/testdir_bk` を確認 | 2026-09-08 | Kiro | - | ○ | |
| D-07 | ディレクトリ削除（空） | 空ディレクトリが正常に削除されること | `rmdir /tmp/testdir_bk` 実行後 `ls /tmp/testdir_bk 2>&1` | 0 | `No such file or directory` が出力される（ディレクトリが削除されている） | 0 | RC=0、`[INFO] ディレクトリ削除（空）完了: /tmp/testdir_bk` を確認 | 2026-09-08 | Kiro | - | ○ | |
| D-08 | ディレクトリ削除（非空・エラー確認） | ファイルを含むディレクトリへの rmdir 実行時に `Directory not empty` が出力され、シェルが異常終了しないこと | ファイルを含む `/tmp/testdir` に対して `rmdir /tmp/testdir` を実行 | 1 | Linux から `rmdir: failed to remove '/tmp/testdir': Directory not empty` が出力される／シェルは異常終了せず次ステップへ続行する | 1 | RC=1、`Directory not empty` 出力を確認。シェルは続行し `[INFO] ディレクトリ削除（非空）エラー確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | ステップ8前処理で dummy.txt を作成し非空状態にして実施 |
| D-09 | ディレクトリ強制削除 | ファイルを含むディレクトリが `-rf` オプションで強制削除されること | `rm -rf /tmp/testdir` 実行後 `ls /tmp/testdir 2>&1` | 0 | `No such file or directory` が出力される（ディレクトリが削除されている） | 0 | RC=0、`[INFO] ディレクトリ強制削除完了: /tmp/testdir` を確認 | 2026-09-08 | Kiro | - | ○ | |

---

### 3.3 ファイル操作（proc_file）

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| F-00 | WORK_DIR 再作成 | proc_dir で削除した WORK_DIR が proc_file 開始時に自動再作成されること | proc_dir 完了後 `ls -la /tmp/testdir` | 0 | `/tmp/testdir` が `mkdir -p` により再作成されている | 0 | RC=0、`[INFO] ファイル操作処理 開始` 後に処理が続行されたことを確認 | 2026-09-08 | Kiro | - | ○ | proc_dir で削除後に proc_file 内で自動再作成される |
| F-01 | ファイル作成（touch） | touch コマンドで空ファイルが正常に作成されること | `touch /tmp/testdir/test.txt` 実行後 `ls -la /tmp/testdir/test.txt` | 0 | `/tmp/testdir/test.txt` が作成されている | 0 | RC=0、`[INFO] ファイル作成（touch）完了: /tmp/testdir/test.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-02 | ファイル作成（echo） | echo リダイレクトでファイルが作成され内容が書き込まれること | `echo hello > /tmp/testdir/data.txt` 実行後 `ls -la /tmp/testdir/data.txt` | 0 | `/tmp/testdir/data.txt` が作成されている | 0 | RC=0、`[INFO] ファイル作成（echo）完了: /tmp/testdir/data.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-03 | ファイル内容確認 | cat で読み込んだファイル内容に期待する文字列が含まれること | `cat /tmp/testdir/data.txt` | 0 | `hello` が出力される | 0 | RC=0、`hello` が含まれることを確認。`[INFO] ファイル内容確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | |
| F-04 | ファイルコピー | ファイルが正常にコピーされること | `cp /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` 実行後 `ls -la /tmp/testdir/data_bk.txt` | 0 | `/tmp/testdir/data_bk.txt` が作成されている | 0 | RC=0、`[INFO] ファイルコピー完了: /tmp/testdir/data_bk.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-05 | ファイルコピー（上書き） | `-f` オプションで既存ファイルへの上書きコピーが正常に行われること | `cp -f /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` 実行後 `diff /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | 差分なし（上書きコピーが成功している） | 0 | RC=0、`[INFO] ファイルコピー（上書き）完了: /tmp/testdir/data_bk.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-06 | ファイル移動（リネーム） | ファイルが移動（リネーム）され、移動元が消え移動先が作成されること | `mv /tmp/testdir/test.txt /tmp/testdir/renamed.txt` 実行後確認 | 0 | `test.txt` が存在せず `renamed.txt` が存在する | 0 | RC=0、`[INFO] ファイル移動（リネーム）完了: /tmp/testdir/renamed.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-07 | ファイル削除 | 指定ファイルが正常に削除されること | `rm /tmp/testdir/data_bk.txt` 実行後 `ls /tmp/testdir/data_bk.txt 2>&1` | 0 | `No such file or directory` が出力される（ファイルが削除されている） | 0 | RC=0、`[INFO] ファイル削除完了: /tmp/testdir/data_bk.txt` を確認 | 2026-09-08 | Kiro | - | ○ | |
| F-08 | ファイル削除（不存在・エラー確認） | 存在しないファイルへの rm 実行時に `No such file or directory` が出力され、シェルが異常終了しないこと | 存在しない `/tmp/testdir/notexist.txt` に対して `rm` を実行 | 1 | Linux から `rm: cannot remove '/tmp/testdir/notexist.txt': No such file or directory` が出力される／シェルは異常終了せず次ステップへ続行する | 1 | RC=1、`No such file or directory` 出力を確認。シェルは続行し `[INFO] ファイル削除（不存在）エラー確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | |

---

### 3.4 サービス操作（proc_service）

> **S-05 RC=3 の根拠：**  
> `systemctl is-active` は サービスが `active` 以外の状態のとき RC=3 を返す（systemd 仕様）。RC=1 はシェル汎用エラーコードであり停止状態を意味しない。

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| S-01 | サービス起動 | `systemctl start` でサービスが正常に起動すること | `systemctl start nginx` 実行後 `systemctl is-active nginx` | 0 | nginx が起動し `active` が出力される | 0 | RC=0、`[INFO] サービス起動完了: nginx` を確認 | 2026-09-08 | Kiro | - | ○ | |
| S-02 | サービス起動失敗確認（Failed なし） | 起動済みサービスへの再起動コマンド実行時に `Failed` が出力されず、シェルが異常終了しないこと | 起動済み nginx に対して再度 `systemctl start nginx` を実行 | 0 | 出力に `Failed` が含まれない／シェルは異常終了せず次ステップへ続行する | 0 | RC=0、出力に `Failed` なし。`[INFO] サービス起動失敗確認完了` を出力 | 2026-09-08 | Kiro | - | ○ | |
| S-03 | 起動確認 | `is-active` コマンドでサービスが active 状態であることが確認できること | `systemctl is-active nginx` | 0 | `active` が1行で出力される | 0 | RC=0、`active` 出力を確認。`[INFO] 起動確認完了: nginx` を出力 | 2026-09-08 | Kiro | - | ○ | |
| S-04 | サービス停止 | `systemctl stop` でサービスが正常に停止すること | `systemctl stop nginx` 実行後 `systemctl is-active nginx` | 3 | nginx が停止し `inactive` が出力される | 0 | RC=0（stop自体は正常終了）、`[INFO] サービス停止完了: nginx` を確認 | 2026-09-08 | Kiro | - | ○ | |
| S-05 | 停止確認 | `is-active` コマンドで inactive 状態が確認でき RC=3（systemd 仕様の停止コード）が返ること | `systemctl is-active nginx`（停止後） | 3 | `inactive` が1行で出力される | 3 | RC=3、`inactive` 出力を確認。`[INFO] 停止確認完了: nginx` を出力 | 2026-09-08 | Kiro | - | ○ | RC=1 ではなく RC=3 が停止を意味する（systemd 仕様） |
| S-06 | サービス状態確認 | `status` コマンドの出力に `Active:` 行が含まれ、サービス状態が確認できること | `systemctl status nginx` | - | 出力に `Active:` の行が含まれる | 3 | RC=3（停止状態のため）、出力に `Active:` 行が含まれることを確認。`[INFO] サービス状態確認完了: nginx` を出力 | 2026-09-08 | Kiro | - | ○ | RC は状態により変わるため `-` |

---

### 3.5 一覧表示・権限変更（proc_misc）

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| M-01 | 一覧表示 | `ls -la` の出力に `total` 行が含まれ、一覧表示が正常に動作すること | `ls -la /tmp` | 0 | 出力の先頭に `total` で始まる行が含まれる | 0 | RC=0、`total` 行を確認。`[INFO] 一覧表示完了: /tmp` を出力 | 2026-09-08 | Kiro | - | ○ | |
| M-02 | ファイル権限変更 | `chmod 755` でファイルに実行権限が正常に付与されること | `chmod 755 /tmp/testfile.sh` 実行後 `ls -la /tmp/testfile.sh` | 0 | パーミッション表示が `-rwxr-xr-x` になっている | 0 | RC=0、`[INFO] ファイル権限変更完了: /tmp/testfile.sh -> 755` を確認 | 2026-09-08 | Kiro | - | ○ | 実行前の権限が `644` であることを確認してから実施 |
| M-03 | 権限変更前後の差異 | 権限変更前後でパーミッションが 644→755 に変化していること | 変更前後の `ls -la /tmp/testfile.sh` 出力を比較 | - | 変更前は `-rw-r--r--`（644）、変更後は `-rwxr-xr-x`（755）になっている | - | deploy_test.sh で `chmod 644` として配置済み。CommandA.sh で `chmod 755` を実行。644→755 の変化を確認 | 2026-09-08 | Kiro | - | ○ | |

---

### 3.6 エラー異常終了確認（エラーハンドリング）

| No | 試験観点 | 試験項目 | 実行コマンド / 操作 | 期待するRC | 期待する出力/動作 | 実際のRC | 実際の出力 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------------|-----------|----------------|---------|-----------|--------|--------|--------|------|------|
| A-01 | E001 異常終了（ログDIR書き込み不可） | ログディレクトリへの書き込みが不可な場合に E001 エラーを出力して RC=1 で異常終了すること | `/var/log/release` を `chmod 444` してから `bash CommandA.sh` | 1 | 標準エラー出力に `[ERROR] E001: ログディレクトリ書き込み権限なし: /var/log/release` が出力され RC=1 で終了する（ログファイルへの書き込みは不可のため生成されない） | 1 | RC=1、標準エラーに `[ERROR] E001: ログディレクトリ書き込み権限なし: /var/log/release` が出力され異常終了。ログファイルは生成されない（書き込み不可のため） | 2026-09-08 | Kiro | - | ○ | ec2-userで実行。試験後に `chmod 755` で権限復元済み |
| A-02 | E002 異常終了（systemctl 不存在） | `systemctl` が存在しない環境で E002 エラーを出力して RC=1 で異常終了すること | `systemctl` を一時退避して `bash CommandA.sh` | 1 | シェル実行ログおよび標準出力に `[ERROR] E002: systemctl コマンド不存在` が出力され RC=1 で終了する（TeraTermログ取得時は画面上にも出力される） | 1 | RC=1、`CommandA_20260908084345.log` に `[ERROR] E002: systemctl コマンド不存在` が記録され異常終了 | 2026-09-08 | Kiro | - | ○ | `/usr/bin/systemctl` を一時退避後に実行。試験後にsystemctl復元済み |

---

### 3.7 正常終了確認

| No | 試験観点 | 試験項目 | 確認方法 | 期待する動作 | 実際の動作 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|---------|------------|-----------|--------|--------|--------|------|------|
| Z-01 | シェル正常終了（RC=0） | シェル全体が RC=0 で正常終了すること | `echo $?`（CommandA.sh 実行直後） | `0` が出力される | `0` が出力された | 2026-09-08 | Kiro | - | ○ | |
| Z-02 | 正常終了ログ記録 | 正常終了メッセージがシェル実行ログに記録されること | `grep "処理正常終了" /var/log/release/CommandA_*.log` | `[INFO]  CommandA.sh 処理正常終了` が記録されている | `[2026-09-08 08:42:25] [INFO]  CommandA.sh 処理正常終了` が記録されていた | 2026-09-08 | Kiro | - | ○ | |
| Z-03 | CMD ログ件数（設計通り 26 件） | 全コマンドの CMD ログが設計通り 26 件記録されていること（処理漏れ・余分な処理がないこと） | `grep "\[CMD\]" /var/log/release/CommandA_detail_*.log \| wc -l` | 26 件の `[CMD]` ログが記録されている（`CommandA.sh` 内の `log_cmd` 呼び出し件数と一致） | 26件を確認 | 2026-09-08 | Kiro | - | ○ | |
| Z-04 | RC ログ件数（CMD と 1 対 1） | CMD ログと RC ログが 1 対 1 で対応して記録されていること | `grep "\[RC\]" /var/log/release/CommandA_detail_*.log \| wc -l` | `[CMD]` と同数（26 件）の `[RC]` ログが記録されている | 26件を確認 | 2026-09-08 | Kiro | - | ○ | |
| Z-05 | ERROR ログ未出力（正常時） | 正常実行時に ERROR ログが出力されていないこと | `grep "\[ERROR\]" /var/log/release/CommandA_*.log` | `[ERROR]` が 1 件も記録されていない | `[ERROR]` が 0 件であることを確認 | 2026-09-08 | Kiro | - | ○ | |

---

## 4. 証跡取得一覧

| 証跡種別 | ファイル名 | 取得方法 |
|---------|-----------|---------|
| Teratermログ | Teraterm操作ログ（自動付番） | Teraterm設定によりセッションログとして自動保存 |
| シェル実行ログ（正常系） | `CommandA_20260908084224.log` | Kiroによりローカルに取得済み |
| 詳細ログ（正常系） | `CommandA_detail_20260908084224.log` | Kiroによりローカルに取得済み |
| シェル実行ログ（A-02異常系） | `CommandA_20260908084345.log` | Kiroによりローカルに取得済み |
| 詳細ログ（A-02異常系） | `CommandA_detail_20260908084345.log` | Kiroによりローカルに取得済み |
| A-01証跡 | ログファイルなし（書き込み不可のため生成されない） | 標準エラー出力で `[ERROR] E001:` を確認済み |
