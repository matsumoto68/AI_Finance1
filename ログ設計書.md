# ログ設計書

## リリース作業自動化 - シェルスクリプト

---

> **【共通化ドキュメント】**  
> 本書は全プロジェクト共通のログ設計標準テンプレートとして管理する。  
> 「共通ルール」セクションは全プロジェクトで統一して適用すること。  
> 「プロジェクト固有設定」セクションは各プロジェクトで上書き・追記すること。

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| ドキュメント種別 | ログ設計書（共通テンプレート） |
| 適用範囲 | リリース作業自動化シェルスクリプト 全プロジェクト共通 |
| プロジェクト固有情報 | 本書「4. プロジェクト固有設定」セクションを参照 |
| 参照資料 | 外部設計書.md、内部設計書.md、ディレクトリ定義書.md |

---

## 2. 【共通ルール】ログ種別一覧

> 全プロジェクト共通。変更不可。

| ログ種別 | 内容 | 対応する証跡 |
|---------|------|------------|
| Teratermログ | コマンド実行の全操作ログ（画面表示） | コマンドログ（Teratermログ） |
| シェル実行ログ | INFO/ERROR メッセージ出力 | シェル実行ログ（メッセージ出力） |
| 詳細ログ | コマンド・ReturnCode 詳細記録 | 詳細ログ（コマンド実行結果） |

---

## 3. 【共通ルール】ログファイル命名規則

> 全プロジェクト共通。変更不可。

| 区分 | 規則 |
|------|------|
| シェル実行ログ | `{シェル名}_{YYYYMMDDHHMMSS}.log` |
| 詳細ログ | `{シェル名}_detail_{YYYYMMDDHHMMSS}.log` |

- `{シェル名}`：実行シェルのファイル名（拡張子なし）
- `{YYYYMMDDHHMMSS}`：実行開始日時（年月日時分秒）
- 同日複数回実行しても上書きされず、実行ごとに別ファイルとして保存される

---

## 4. 【共通ルール】ログレベル定義

> 全プロジェクト共通。変更不可。

| レベル | 用途 | 出力先 |
|--------|------|--------|
| `INFO` | 処理の開始・終了・正常実行結果 | シェル実行ログ・標準出力 |
| `ERROR` | エラー発生・異常終了 | シェル実行ログ・標準エラー出力 |
| `CMD` | 実行コマンドの記録 | 詳細ログ |
| `RC` | ReturnCode の記録 | 詳細ログ |

---

## 5. 【共通ルール】ログ出力フォーマット

> 全プロジェクト共通。変更不可。

### 5.1 シェル実行ログ（INFO/ERROR）

```
[YYYY-MM-DD HH:MM:SS] [INFO]  処理内容メッセージ
[YYYY-MM-DD HH:MM:SS] [ERROR] エラーコード: エラーメッセージ
```

### 5.2 詳細ログ（CMD/RC）

```
[YYYY-MM-DD HH:MM:SS] [CMD] 実行コマンド文字列
[YYYY-MM-DD HH:MM:SS] [RC]  ReturnCode=0
```

### 5.3 出力例（正常時）

```
[YYYY-MM-DD HH:MM:SS] [INFO]  処理開始: {シェル名}.sh
[YYYY-MM-DD HH:MM:SS] [CMD]   {実行コマンド}
[YYYY-MM-DD HH:MM:SS] [RC]    ReturnCode=0
[YYYY-MM-DD HH:MM:SS] [INFO]  処理完了: {処理内容}
```

### 5.4 出力例（エラー時）

```
[YYYY-MM-DD HH:MM:SS] [CMD]   {実行コマンド} 2>&1
[YYYY-MM-DD HH:MM:SS] [RC]    ReturnCode=1
[YYYY-MM-DD HH:MM:SS] [ERROR] {エラーコード}: {エラーメッセージ}
```

---

## 6. 【共通ルール】ログ初期化処理（init_log）

> 全プロジェクト共通。変更不可。

| ステップ | 処理内容 |
|---------|---------|
| 1 | ログ格納ディレクトリが存在しない場合、`mkdir -p` で作成 |
| 2 | シェル実行ログ・詳細ログのヘッダー行を出力（実行開始日時・スクリプト名） |

**ヘッダー出力例：**
```
========================================
 スクリプト名: {シェル名}.sh
 実行開始日時: YYYY-MM-DD HH:MM:SS
========================================
```

---

## 7. 【共通ルール】ログ出力関数一覧

> 全プロジェクト共通。関数名・引数・仕様は変更不可。

| 関数名 | 役割 | 引数 | 出力先 |
|--------|------|------|--------|
| `log_info()` | INFO ログ出力 | `$1`：メッセージ | シェル実行ログ・標準出力 |
| `log_error()` | ERROR ログ出力 | `$1`：エラーメッセージ | シェル実行ログ・標準エラー出力 |
| `log_cmd()` | コマンドログ出力 | `$1`：実行コマンド文字列 | 詳細ログ |
| `log_rc()` | ReturnCode ログ出力 | `$1`：ReturnCode 値 | 詳細ログ |

---

## 8. 【共通ルール】ログファイルの権限・保管

> 全プロジェクト共通。変更不可。

| 項目 | 内容 |
|------|------|
| ログファイル権限 | `644`（オーナー：読み書き、グループ/その他：読み取りのみ） |
| 保管期間 | 規定なし（手動管理） |
| 削除 | 自動削除なし（手動削除） |

---

## 9. 【プロジェクト固有設定】CommandA プロジェクト

> 本セクションは CommandA プロジェクト固有の設定。他プロジェクトでは本セクションを差し替えること。

### 9.1 ログファイル名（CommandA）

| 区分 | ファイル名 | 例 |
|------|-----------|---|
| シェル実行ログ | `CommandA_YYYYMMDDHHMMSS.log` | `CommandA_20260825143052.log` |
| 詳細ログ | `CommandA_detail_YYYYMMDDHHMMSS.log` | `CommandA_detail_20260825143052.log` |

### 9.2 ログ格納先（CommandA）

| 種別 | パス | 備考 |
|------|------|------|
| ログ格納ディレクトリ | `/var/log/release/` | 資材配置フォルダ（`/opt/release/scripts/`）とは分離 |
| ディレクトリ権限 | `755` | |

### 9.3 出力例（CommandA 正常時）

```
[2026-08-25 14:30:52] [INFO]  処理開始: CommandA.sh
[2026-08-25 14:30:52] [CMD]   mkdir /tmp/testdir
[2026-08-25 14:30:52] [RC]    ReturnCode=0
[2026-08-25 14:30:52] [INFO]  ディレクトリ作成完了: /tmp/testdir
```

### 9.4 出力例（CommandA エラー時）

```
[2026-08-25 14:30:55] [CMD]   mkdir /tmp/testdir 2>&1
[2026-08-25 14:30:55] [RC]    ReturnCode=1
[2026-08-25 14:30:55] [ERROR] E101: ディレクトリ作成失敗（既存） - mkdir /tmp/testdir
```
