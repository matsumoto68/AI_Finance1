# 結合試験 項目表

## CommandA.sh 結合試験

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 結合試験 項目表 |
| 対象シェル | `CommandA.sh`（全処理フロー通し実行） |
| 参照資料 | 外部設計書.md、内部設計書.md、エラーコード定義書.md、単体試験_項目表.md |
| 前提 | 単体試験が全項目合格していること |

---

## 2. 試験環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 接続方法 | Teraterm（SSH接続） |
| 実行ユーザー | 一般ユーザー（`$` プロンプト） |
| 前提 | `deploy_CommandA.sh` による資材配置完了後に実施 |

---

## 3. 試験方針

結合試験では `CommandA.sh` を**1回の実行で全処理フローを通し実行**し、以下を確認する。

- 各処理ブロック（ディレクトリ・ファイル・サービス・権限変更）が順序通りに実行されること
- シェル全体が正常終了（RC=0）すること
- ログファイルが正しく生成・出力されること
- ファイル/ディレクトリの作成・削除結果が正しいこと

---

## 4. 試験項目一覧

### 4.1 事前準備確認

| No | 試験項目 | 確認方法 | 期待する状態 | 実際の状態 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------|------|
| P-01 | 資材配置確認 | `ls -la /opt/release/scripts/CommandA.sh` | ファイルが存在し権限が `755` である | | | |
| P-02 | 試験データ確認 | `ls /tmp/testfile.sh` | `/tmp/testfile.sh` が存在する | | | 試験データ（test_data.sh）を事前配置すること |
| P-03 | nginx インストール確認 | `systemctl status nginx` | nginx がインストール済みである | | | |

---

### 4.2 全体フロー実行

| No | 試験項目 | 実行コマンド | 期待するRC | 期待する動作 | 実際のRC | 実際の動作 | 合否 | 備考 |
|----|---------|------------|-----------|------------|---------|-----------|------|------|
| I-01 | CommandA.sh 全体実行 | `bash /opt/release/scripts/CommandA.sh` | 0 | 全処理が正常終了し RC=0 で終了する | | | | |

---

### 4.3 処理結果確認（実行後）

#### ディレクトリ・ファイル操作結果

| No | 試験項目 | 確認方法 | 期待する状態 | 実際の状態 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------|------|
| R-01 | `/tmp/testdir` 削除確認 | `ls /tmp/testdir 2>&1` | `No such file or directory` が出力される（削除済み） | | | proc_dir ステップ9で削除 |
| R-02 | `/tmp/testdir_bk` 削除確認 | `ls /tmp/testdir_bk 2>&1` | `No such file or directory` が出力される（削除済み） | | | proc_dir ステップ7で削除 |
| R-03 | `/tmp/testfile.sh` 権限確認 | `ls -la /tmp/testfile.sh` | 権限が `755` である | | | proc_misc ステップ2で変更 |

#### ログ出力確認

| No | 試験項目 | 確認方法 | 期待する内容 | 実際の内容 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------|------|
| L-01 | シェル実行ログ生成確認 | `ls /var/log/release/CommandA_*.log` | ファイルが1件生成されている | | | |
| L-02 | 詳細ログ生成確認 | `ls /var/log/release/CommandA_detail_*.log` | ファイルが1件生成されている | | | |
| L-03 | 正常終了ログ確認 | `grep "処理正常終了" /var/log/release/CommandA_*.log` | `[INFO] CommandA.sh 処理正常終了` が記録されている | | | |
| L-04 | 全 CMD ログ確認 | `grep "\[CMD\]" /var/log/release/CommandA_detail_*.log \| wc -l` | 25件以上の CMD ログが記録されている | | | |
| L-05 | 全 RC ログ確認 | `grep "\[RC\]" /var/log/release/CommandA_detail_*.log \| wc -l` | CMD ログと同数の RC ログが記録されている | | | |
| L-06 | エラーログ未出力確認 | `grep "\[ERROR\]" /var/log/release/CommandA_*.log` | ERROR ログが出力されていない | | | |

---

### 4.4 異常系確認（シェル単独実行）

| No | 試験項目 | 実行コマンド | 期待するRC | 期待する動作 | 実際のRC | 実際の動作 | 合否 | 備考 |
|----|---------|------------|-----------|------------|---------|-----------|------|------|
| A-01 | ログディレクトリ書き込み不可時の異常終了 | ログDIRを読み取り専用にして `bash CommandA.sh` | 1 | `E001` エラーログが出力されRC=1で終了する | | | | 試験後に権限を元に戻すこと |
| A-02 | systemctl 不存在時の異常終了 | PATH から systemctl を除いて実行 | 1 | `E002` エラーログが出力されRC=1で終了する | | | | 試験後に PATH を元に戻すこと |

---

## 5. 証跡取得一覧

| 証跡種別 | ファイル名 | 取得方法 |
|---------|-----------|---------|
| Teratermログ | Teraterm操作ログ（自動付番） | Teraterm設定によりセッションログとして自動保存 |
| シェル実行ログ | `/var/log/release/CommandA_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
| 詳細ログ | `/var/log/release/CommandA_detail_YYYYMMDDHHMMSS.log` | 実行後にKiroで整理・記録 |
| ファイル/ディレクトリ作成・削除結果 | 確認コマンドの出力 | 上記 R-01〜R-03 の確認結果をKiroで整理・記録 |
