# 結合試験 項目表

## CommandA.sh 結合試験

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 結合試験 項目表 |
| 対象シェル | `CommandA.sh`（全処理フロー通し実行） |
| 参照資料 | 外部設計書.md、内部設計書.md、エラーコード定義書.md、単体試験_項目表.md |
| 前提 | 単体試験が全項目合格していること |

---

## 2. 試験環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 接続方法 | Teraterm（SSH接続） |
| 実行ユーザー | 一般ユーザー（`$` プロンプト） |
| 前提 | `deploy_CommandA.sh` による資材配置完了後に実施 |

---

## 3. 試験方針

結合試験では `CommandA.sh` を**1回の実行で全処理フローを通し実行**し、以下を確認する。

- 各処理ブロック（ディレクトリ・ファイル・サービス・権限変更）が設計通りの順序で実行されること
- 処理ブロック間の依存関係（proc_dir 後に proc_file が WORK_DIR を再作成する等）が正しく機能すること
- シェル全体が正常終了（RC=0）すること
- ログファイルが正しく生成・出力されること
- ファイル/ディレクトリの作成・削除結果が正しいこと

---

## 4. 試験項目一覧

### 凡例

| 記号 | 意味 |
|------|------|
| ○ | 正常終了（期待値と一致） |
| × | 異常終了（期待値と不一致） |
| - | 対象外 |

---

### 4.1 事前準備確認

| No | 試験観点 | 試験項目 | 確認方法 | 期待する状態 | 実際の状態 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|---------|------------|-----------|--------|--------|--------|------|------|
| P-01 | 資材配置確認 | 試験実施前にシェルが正しいパスと権限（755）で配置されていること | `ls -la /opt/release/scripts/CommandA.sh` | ファイルが存在し権限が `755` である | `-rwxr-xr-x` で存在確認 | 2026-09-08 | Kiro | - | ○ | |
| P-02 | 試験データ確認 | 試験データ（権限変更テスト用ファイル）が初期権限 644 で事前配置されていること | `ls -la /tmp/testfile.sh` | `/tmp/testfile.sh` が存在し権限が `644` である | `-rw-r--r--` で存在確認 | 2026-09-08 | Kiro | - | ○ | 試験データ（test_data.sh）を事前配置すること |
| P-03 | nginx インストール確認 | サービス操作の前提となる nginx がインストール済みであること | `systemctl status nginx` | nginx がインストール済みである | `Loaded: loaded` を確認 | 2026-09-08 | Kiro | - | ○ | |

---

### 4.2 全体フロー実行

| No | 試験観点 | 試験項目 | 実行コマンド | 期待するRC | 期待する動作 | 実際のRC | 実際の動作 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------------|---------|-----------|--------|--------|--------|------|------|
| I-01 | 全体フロー正常終了 | 全処理ブロックが設計通りの順序で実行され、シェル全体が RC=0 で正常終了すること | `bash /opt/release/scripts/CommandA.sh` | 0 | 全処理が正常終了し RC=0 で終了する | 0 | RC=0。全処理ブロック（proc_dir/proc_file/proc_service/proc_misc）が設計順に実行され正常終了 | 2026-09-08 | Kiro | - | ○ | |

---

### 4.3 処理結果確認（実行後）

#### ディレクトリ・ファイル操作結果

| No | 試験観点 | 試験項目 | 確認方法 | 期待する状態 | 実際の状態 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|---------|------------|-----------|--------|--------|--------|------|------|
| R-01 | `/tmp/testdir` 削除確認 | proc_file のステップ9（強制削除）により `/tmp/testdir` がシェル終了時点で存在しないこと | `ls /tmp/testdir 2>&1` | `No such file or directory` が出力される（削除済み） | `No such file or directory` が出力された | 2026-09-08 | Kiro | - | ○ | proc_file ステップ9（`rm -rf`）で削除 |
| R-02 | `/tmp/testdir_bk` 削除確認 | proc_dir のステップ7（空削除）により `/tmp/testdir_bk` がシェル終了時点で存在しないこと | `ls /tmp/testdir_bk 2>&1` | `No such file or directory` が出力される（削除済み） | `No such file or directory` が出力された | 2026-09-08 | Kiro | - | ○ | proc_dir ステップ7（`rmdir`）で削除 |
| R-03 | `/tmp/testfile.sh` 権限変更確認 | proc_misc のステップ2（chmod）により `/tmp/testfile.sh` の権限が 755 に変更されていること | `ls -la /tmp/testfile.sh` | パーミッション表示が `-rwxr-xr-x`（755）である | `-rwxr-xr-x`（755）を確認 | 2026-09-08 | Kiro | - | ○ | proc_misc ステップ2（`chmod 755`）で変更 |

#### ログ出力確認

| No | 試験観点 | 試験項目 | 確認方法 | 期待する内容 | 実際の内容 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|---------|------------|-----------|--------|--------|--------|------|------|
| L-01 | シェル実行ログ生成 | 全体実行によりシェル実行ログが1件生成されること | `ls /var/log/release/CommandA_*.log` | ファイルが1件生成されている | `CommandA_20260908094917.log` 生成確認 | 2026-09-08 | Kiro | - | ○ | |
| L-02 | 詳細ログ生成 | 全体実行により詳細ログが1件生成されること | `ls /var/log/release/CommandA_detail_*.log` | ファイルが1件生成されている | `CommandA_detail_20260908094917.log` 生成確認 | 2026-09-08 | Kiro | - | ○ | |
| L-03 | 正常終了ログ記録 | 全体実行の正常終了メッセージがシェル実行ログに記録されること | `grep "処理正常終了" /var/log/release/CommandA_*.log` | `[INFO]  CommandA.sh 処理正常終了` が記録されている | `[2026-09-08 09:49:18] [INFO]  CommandA.sh 処理正常終了` を確認 | 2026-09-08 | Kiro | - | ○ | |
| L-04 | CMD ログ件数（設計通り 27 件） | 全コマンドの CMD ログが設計通り 27 件記録されていること（処理漏れ・余分な処理がないこと） | `grep "\[CMD\]" /var/log/release/CommandA_detail_*.log \| wc -l` | 27 件の `[CMD]` ログが記録されている | 27件を確認 | 2026-09-08 | Kiro | - | ○ | |
| L-05 | RC ログ件数（CMD と 1 対 1） | CMD ログと RC ログが 1 対 1 で対応して記録されていること | `grep "\[RC\]" /var/log/release/CommandA_detail_*.log \| wc -l` | `[CMD]` と同数（27 件）の `[RC]` ログが記録されている | 27件を確認 | 2026-09-08 | Kiro | - | ○ | |
| L-06 | ERROR ログ未出力（正常時） | 正常実行時に ERROR ログが出力されていないこと | `grep "\[ERROR\]" /var/log/release/CommandA_*.log` | `[ERROR]` が 1 件も記録されていない | 0件を確認 | 2026-09-08 | Kiro | - | ○ | |

---

### 4.4 異常系確認

| No | 試験観点 | 試験項目 | 実行コマンド | 期待するRC | 期待する動作 | 実際のRC | 実際の動作 | 実施日 | 対応者 | 確認者 | 合否 | 備考 |
|----|---------|---------|------------|-----------|------------|---------|-----------|--------|--------|--------|------|------|
| A-01 | E001 異常終了（ログDIR書き込み不可） | ログディレクトリへの書き込みが不可な場合に E001 エラーを出力して RC=1 で異常終了すること | `/var/log/release` を `chmod 444` してから `bash /opt/release/scripts/CommandA.sh` | 1 | 標準エラー出力に `[ERROR] E001: ログディレクトリ書き込み権限なし: /var/log/release` が出力され RC=1 で終了する（ログファイルへの書き込みは不可のため生成されない） | 1 | RC=1、標準エラーに `[ERROR] E001: ログディレクトリ書き込み権限なし: /var/log/release` が出力され異常終了。ログファイルは生成されない | 2026-09-08 | Kiro | - | ○ | 試験後に `chmod 755` で権限復元済み |
| A-02 | E002 異常終了（systemctl 不存在） | `systemctl` が存在しない環境で E002 エラーを出力して RC=1 で異常終了すること | `PATH` を `systemctl` を含まないパスに変更して `bash /opt/release/scripts/CommandA.sh` | 1 | シェル実行ログおよび標準出力に `[ERROR] E002: systemctl コマンド不存在` が出力され RC=1 で終了する（TeraTermログ取得時は画面上にも出力される） | 1 | RC=1、`CommandA_20260908095024.log` に `[ERROR] E002: systemctl コマンド不存在` が記録され異常終了 | 2026-09-08 | Kiro | - | ○ | `/usr/bin/systemctl` を一時退避後に実行。試験後にsystemctl・権限復元済み |

---

## 5. 証跡取得一覧

| 証跡種別 | ファイル名 | 取得方法 |
|---------|-----------|---------|
| シェル実行ログ（正常系） | `UT試験Log/IT正常系/CommandA_20260908094917.log` | Kiroによりローカルに取得済み |
| 詳細ログ（正常系） | `UT試験Log/IT正常系/CommandA_detail_20260908094917.log` | Kiroによりローカルに取得済み |
| A-01証跡（標準エラー） | `UT試験Log/IT異常系/CommandA_A01_stderr.log` | stderrリダイレクトで取得済み |
| シェル実行ログ（A-02異常系） | `UT試験Log/IT異常系/CommandA_20260908095024.log` | Kiroによりローカルに取得済み |
| 詳細ログ（A-02異常系） | `UT試験Log/IT異常系/CommandA_detail_20260908095024.log` | Kiroによりローカルに取得済み |
| ファイル/ディレクトリ削除・権限変更結果 | R-01〜R-03 の確認コマンド出力（`ls` による不存在確認・権限表示） | Kiroによりサーバ上で確認済み |
