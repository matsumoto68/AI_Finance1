# 外部設計書

## リリース作業自動化 - シェルスクリプト

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | 外部設計書 |
| 対象 | CommandA 自動化シェルスクリプト |
| 参照資料 | test_data_CommandA.xlsx（手動コマンド手順書） |

---

## 2. システム概要

### 2.1 目的

担当者から展開された手動Linuxコマンド手順書（`test_data_CommandA.xlsx`）に記載された全25コマンドをシェルスクリプト化し、リリース作業を自動実行する。

### 2.2 実行環境

| 項目 | 内容 |
|------|------|
| OS | Linux |
| シェル | bash |
| 実行ユーザー | 一般ユーザー（$プロンプト）/ rootユーザー（#プロンプト） |
| 接続方法 | Teraterm（SSH接続） |

### 2.3 システム構成

```
担当者
  │
  │ 手動コマンド手順書（test_data_CommandA.xlsx）を展開
  ▼
開発チーム
  │
  │ Kiroを使用してシェルスクリプトを自動生成・レビュー
  ▼
シェルスクリプト（CommandA.sh）
  │
  │ 資材配置シェルによりLinux環境へ配置
  ▼
Linux環境（実行サーバ）
  │
  │ シェル実行
  ▼
実行ログ出力
```

---

## 3. シェルスクリプト仕様

### 3.1 ファイル構成

| ファイル名 | 種別 | 説明 |
|-----------|------|------|
| `CommandA.sh` | メインシェル | 全自動化コマンドを実行するメインスクリプト |
| `deploy_CommandA.sh` | 資材配置シェル | `CommandA.sh` をLinux環境の所定ディレクトリへ配置する |

### 3.2 配置ディレクトリ

| 種別 | パス | 説明 |
|------|------|------|
| スクリプト配置先 | `/opt/release/scripts/` | シェルスクリプトの配置ディレクトリ |
| ログ出力先 | `/opt/release/logs/` | 実行ログの出力ディレクトリ |
| 作業ディレクトリ | `/tmp/testdir/` | ファイル・ディレクトリ操作の対象ディレクトリ |

### 3.3 実行権限

| ファイル | 権限 | 説明 |
|----------|------|------|
| `CommandA.sh` | `755` | オーナー実行可・グループ/その他は読み取り・実行可 |
| `deploy_CommandA.sh` | `755` | 同上 |

---

## 4. 処理概要

### 4.1 処理フロー

```
開始
  │
  ├─ [1] 事前チェック（実行環境・権限確認）
  │
  ├─ [2] ディレクトリ操作処理
  │     ├─ ディレクトリ作成
  │     ├─ ディレクトリ移動
  │     ├─ ディレクトリコピー
  │     └─ ディレクトリ削除
  │
  ├─ [3] ファイル操作処理
  │     ├─ ファイル作成
  │     ├─ ファイル内容確認
  │     ├─ ファイルコピー
  │     ├─ ファイル移動（リネーム）
  │     └─ ファイル削除
  │
  ├─ [4] サービス操作処理
  │     ├─ サービス起動
  │     ├─ 起動確認
  │     ├─ サービス停止
  │     ├─ 停止確認
  │     └─ サービス状態確認
  │
  ├─ [5] 一覧表示・権限変更処理
  │
  └─ 終了（正常終了 / エラー終了）
```

### 4.2 自動化対象コマンド一覧

`test_data_CommandA.xlsx` に記載された全コマンドを自動化対象とする。

#### カテゴリ1：ディレクトリ操作

| 項番 | コマンドの内容 | Linuxコマンド | 正常ReturnCode | エラー条件 |
|------|--------------|--------------|---------------|------------|
| 1 | ディレクトリ作成 | `mkdir /tmp/testdir` | 0 | - |
| 2 | ディレクトリ作成（既存） | `mkdir /tmp/testdir` | 1 | `File exists` を含む場合エラー |
| 3 | ディレクトリ作成（権限なし） | `mkdir /root/noperm` | 1 | `Permission denied` を含む場合エラー |
| 7 | ディレクトリ移動 | `cd /tmp/testdir` | 0 | - |
| 8 | ディレクトリ移動（存在しない） | `cd /tmp/notexist` | 1 | `No such file or directory` を含む場合エラー |
| 11 | ディレクトリコピー | `cp -r /tmp/testdir /tmp/testdir_bk` | 0 | - |
| 15 | ディレクトリ削除（空） | `rmdir /tmp/testdir_bk` | 0 | - |
| 16 | ディレクトリ削除（非空） | `rmdir /tmp/testdir` | 1 | `Directory not empty` を含む場合エラー |
| 17 | ディレクトリ強制削除 | `rm -rf /tmp/testdir` | 0 | - |

#### カテゴリ2：ファイル操作

| 項番 | コマンドの内容 | Linuxコマンド | 正常ReturnCode | エラー条件 |
|------|--------------|--------------|---------------|------------|
| 4 | ファイル作成（touch） | `touch /tmp/testdir/test.txt` | 0 | - |
| 5 | ファイル作成（echo） | `echo hello > /tmp/testdir/data.txt` | 0 | - |
| 6 | ファイル内容確認 | `cat /tmp/testdir/data.txt` | 0 | `hello` が含まれない場合エラー |
| 9 | ファイルコピー | `cp /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | - |
| 10 | ファイルコピー（上書き） | `cp -f /tmp/testdir/data.txt /tmp/testdir/data_bk.txt` | 0 | - |
| 12 | ファイル移動（リネーム） | `mv /tmp/testdir/test.txt /tmp/testdir/renamed.txt` | 0 | - |
| 13 | ファイル削除 | `rm /tmp/testdir/data_bk.txt` | 0 | - |
| 14 | ファイル削除（存在しない） | `rm /tmp/testdir/notexist.txt` | 1 | `No such file or directory` を含む場合エラー |

#### カテゴリ3：サービス操作

| 項番 | コマンドの内容 | Linuxコマンド | 正常ReturnCode | エラー条件 |
|------|--------------|--------------|---------------|------------|
| 18 | サービス起動 | `systemctl start nginx` | 0 | - |
| 19 | サービス起動（失敗） | `systemctl start nginx` | 1 | `Failed to start` / `Failed` を含む場合エラー |
| 20 | 起動確認 | `systemctl is-active nginx` | 0 | `active` が含まれない場合エラー |
| 21 | サービス停止 | `systemctl stop nginx` | 0 | - |
| 22 | 停止確認 | `systemctl is-active nginx` | 3 | `inactive` が含まれない場合エラー |
| 23 | サービス状態確認 | `systemctl status nginx` | - | `Active` が含まれない場合エラー |

#### カテゴリ4：一覧表示・権限変更

| 項番 | コマンドの内容 | Linuxコマンド | 正常ReturnCode | エラー条件 |
|------|--------------|--------------|---------------|------------|
| 24 | 一覧表示（ls） | `ls -la /tmp` | 0 | `total` が含まれない場合エラー |
| 25 | ファイル権限変更 | `chmod 755 /tmp/testfile.sh` | 0 | - |

---

## 5. 入出力仕様

### 5.1 入力

| 種別 | 内容 | 備考 |
|------|------|------|
| 実行コマンド | `bash CommandA.sh` | Teraterm経由またはLinux環境上で直接実行 |
| 実行引数 | なし（引数なし実行） | 将来拡張用に引数受付を設計可 |

### 5.2 出力（ログ）

| ログ種別 | 出力先 | 内容 |
|----------|--------|------|
| Teratermログ | Teraterm操作ログファイル | コマンド実行の全操作ログ |
| シェル実行ログ | `/opt/release/logs/CommandA_YYYYMMDD.log` | シェル内のメッセージ出力（echo等） |
| 詳細ログ | `/opt/release/logs/CommandA_detail_YYYYMMDD.log` | コマンドの実行結果・ReturnCode |

### 5.3 ログ出力フォーマット

```
[YYYY-MM-DD HH:MM:SS] [INFO/ERROR] 処理内容メッセージ
[YYYY-MM-DD HH:MM:SS] [CMD] 実行コマンド
[YYYY-MM-DD HH:MM:SS] [RC] ReturnCode=0
```

---

## 6. エラーハンドリング

### 6.1 エラー検出方式

各コマンド実行後に以下の2軸でエラーを検出する。

| 検出方式 | 内容 |
|----------|------|
| ReturnCode チェック | コマンドの終了コード（$?）を確認。正常値以外はエラーとして処理 |
| 結果出力確認文字チェック | コマンド出力に特定の文字列が含まれるかを確認（`grep`を使用） |

### 6.2 エラー発生時の動作

| 動作 | 内容 |
|------|------|
| ログ出力 | エラー内容・コマンド名・ReturnCode を詳細ログに記録 |
| 処理中断 | エラー発生時点で以降の処理を中断し、シェルを終了する（`exit 1`） |
| メッセージ出力 | エラー内容をシェル実行ログに出力する |

### 6.3 エラーメッセージ一覧

| 項番 | エラー内容 | 確認文字 | 対処 |
|------|-----------|---------|------|
| 2 | ディレクトリ既存 | `File exists` | エラーログ出力・処理中断 |
| 3 | 権限なし | `Permission denied` | エラーログ出力・処理中断 |
| 8 | ディレクトリ不存在 | `No such file or directory` | エラーログ出力・処理中断 |
| 14 | ファイル不存在 | `No such file or directory` | エラーログ出力・処理中断 |
| 16 | ディレクトリ非空 | `Directory not empty` | エラーログ出力・処理中断 |
| 19 | サービス起動失敗 | `Failed to start` / `Failed` | エラーログ出力・処理中断 |

---

## 7. 資材配置シェル仕様

### 7.1 概要

試験実施前に `CommandA.sh` をLinux環境の所定ディレクトリへ配置するためのシェルスクリプト。

### 7.2 処理内容

```
開始
  │
  ├─ [1] 配置先ディレクトリの存在確認
  │       存在しない場合 → ディレクトリを作成してから続行
  │
  ├─ [2] CommandA.sh を配置先へコピー
  │
  ├─ [3] 実行権限の付与（chmod 755）
  │
  └─ 終了
```

### 7.3 入出力

| 種別 | 内容 |
|------|------|
| 入力 | `CommandA.sh`（カレントディレクトリに配置） |
| 配置先 | `/opt/release/scripts/CommandA.sh` |
| 実行権限 | `755` |

---

## 8. 制約・前提条件

| # | 区分 | 内容 |
|---|------|------|
| 1 | 前提 | Linuxサーバへの SSH 接続が確立されていること |
| 2 | 前提 | 実行ユーザーが `/tmp/testdir` への書き込み権限を持つこと |
| 3 | 前提 | `systemctl` コマンドが使用可能であること（nginx がインストール済みであること） |
| 4 | 制約 | `/root/` 配下へのディレクトリ作成は権限エラーが発生する（意図的なエラーテストケース） |
| 5 | 制約 | シェルスクリプトはbash構文に準拠すること |
| 6 | 制約 | 機密情報（本番IPアドレス・パスワード等）はシェルスクリプトにハードコードしない |
