# ディレクトリ定義書

## リリース作業自動化 - シェルスクリプト

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | ディレクトリ定義書 |
| 対象 | CommandA 自動化シェルスクリプト |
| 参照資料 | 外部設計書.md、内部設計書.md |

---

## 2. ディレクトリ構成

### 2.1 Linux環境（実行サーバ）

```
/
├── opt/
│   └── release/
│       ├── scripts/                  # スクリプト配置ディレクトリ
│       │   ├── CommandA.sh           # メインシェルスクリプト
│       │   └── deploy_CommandA.sh    # 資材配置シェル
│       └── logs/                     # ログ出力ディレクトリ
│           ├── CommandA_YYYYMMDD.log         # シェル実行ログ
│           └── CommandA_detail_YYYYMMDD.log  # 詳細ログ
└── tmp/
    ├── testdir/                      # 作業ディレクトリ（テスト用）
    │   ├── test.txt                  # ファイル作成（touch）対象
    │   ├── data.txt                  # データファイル
    │   ├── data_bk.txt               # データバックアップファイル
    │   └── renamed.txt               # リネーム後ファイル
    ├── testdir_bk/                   # バックアップディレクトリ（コピー先）
    └── testfile.sh                   # 権限変更対象ファイル
```

---

## 3. ディレクトリ定義

### 3.1 `/opt/release/scripts/`

| 項目 | 内容 |
|------|------|
| パス | `/opt/release/scripts/` |
| 用途 | シェルスクリプトの配置ディレクトリ |
| 作成タイミング | 資材配置シェル（`deploy_CommandA.sh`）実行時に存在しない場合自動作成 |
| 権限 | `755`（オーナー：読み書き実行、グループ/その他：読み取り・実行） |
| 備考 | 本番・試験環境共通パス |

### 3.2 `/opt/release/logs/`

| 項目 | 内容 |
|------|------|
| パス | `/opt/release/logs/` |
| 用途 | シェルスクリプトの実行ログ出力先 |
| 作成タイミング | `CommandA.sh` の初期化処理（init_log）実行時に存在しない場合自動作成 |
| 権限 | `755` |
| 備考 | ログファイルはYYYYMMDD付きで日次生成 |

### 3.3 `/tmp/testdir/`

| 項目 | 内容 |
|------|------|
| パス | `/tmp/testdir/` |
| 用途 | ファイル・ディレクトリ操作コマンドの作業ディレクトリ |
| 作成タイミング | `CommandA.sh` のディレクトリ操作処理（項番1）で作成 |
| 権限 | `755` |
| 備考 | 処理の最後（項番17）で `rm -rf` により削除される |

### 3.4 `/tmp/testdir_bk/`

| 項目 | 内容 |
|------|------|
| パス | `/tmp/testdir_bk/` |
| 用途 | `testdir` のバックアップ（ディレクトリコピー先） |
| 作成タイミング | `CommandA.sh` のディレクトリコピー処理（項番11）で作成 |
| 権限 | コピー元と同じ `755` |
| 備考 | ディレクトリ削除処理（項番15）で削除される |

---

## 4. ファイル定義

### 4.1 シェルスクリプトファイル

| ファイル名 | 配置パス | 権限 | 用途 |
|-----------|---------|------|------|
| `CommandA.sh` | `/opt/release/scripts/CommandA.sh` | `755` | メインシェルスクリプト |
| `deploy_CommandA.sh` | `/opt/release/scripts/deploy_CommandA.sh` | `755` | 資材配置シェル |

### 4.2 ログファイル

| ファイル名 | 配置パス | 用途 | 命名規則 |
|-----------|---------|------|---------|
| シェル実行ログ | `/opt/release/logs/CommandA_YYYYMMDD.log` | INFO/ERRORメッセージ出力 | `CommandA_` + 実行日（YYYYMMDD） + `.log` |
| 詳細ログ | `/opt/release/logs/CommandA_detail_YYYYMMDD.log` | コマンド・ReturnCode詳細記録 | `CommandA_detail_` + 実行日（YYYYMMDD） + `.log` |

### 4.3 作業ファイル（テスト用）

| ファイル名 | 配置パス | 作成コマンド | 用途 |
|-----------|---------|------------|------|
| `test.txt` | `/tmp/testdir/test.txt` | `touch` | ファイル作成・移動テスト用 |
| `data.txt` | `/tmp/testdir/data.txt` | `echo hello >` | ファイル内容確認・コピーテスト用 |
| `data_bk.txt` | `/tmp/testdir/data_bk.txt` | `cp` | コピー先・削除テスト用 |
| `renamed.txt` | `/tmp/testdir/renamed.txt` | `mv` | リネーム後ファイル |
| `testfile.sh` | `/tmp/testfile.sh` | 事前配置 | 権限変更テスト用 |

---

## 5. 命名規則

### 5.1 シェルスクリプト

| 区分 | 規則 | 例 |
|------|------|----|
| メインシェル | `{コマンドID}.sh` | `CommandA.sh` |
| 資材配置シェル | `deploy_{コマンドID}.sh` | `deploy_CommandA.sh` |

### 5.2 ログファイル

| 区分 | 規則 | 例 |
|------|------|----|
| シェル実行ログ | `{コマンドID}_YYYYMMDD.log` | `CommandA_20260825.log` |
| 詳細ログ | `{コマンドID}_detail_YYYYMMDD.log` | `CommandA_detail_20260825.log` |

### 5.3 作業ディレクトリ・ファイル

| 区分 | 規則 |
|------|------|
| 作業ディレクトリ | `/tmp/` 配下に作成する |
| バックアップ | 元のディレクトリ名に `_bk` を付与 |
| バックアップファイル | 元のファイル名（拡張子なし）に `_bk` を付与 |

---

## 6. 権限定義

| 対象 | 権限値 | オーナー | グループ | その他 |
|------|--------|---------|---------|--------|
| シェルスクリプト（.sh） | `755` | rwx | r-x | r-x |
| ログディレクトリ | `755` | rwx | r-x | r-x |
| ログファイル（.log） | `644` | rw- | r-- | r-- |
| 作業ディレクトリ | `755` | rwx | r-x | r-x |

---

## 7. ディレクトリ作成・削除タイミング

| ディレクトリ/ファイル | 作成タイミング | 削除タイミング |
|---------------------|--------------|--------------|
| `/opt/release/scripts/` | deploy_CommandA.sh 実行時（存在しない場合） | 手動削除（自動削除なし） |
| `/opt/release/logs/` | CommandA.sh init_log 実行時（存在しない場合） | 手動削除（自動削除なし） |
| `/tmp/testdir/` | CommandA.sh 項番1（mkdir） | CommandA.sh 項番17（rm -rf） |
| `/tmp/testdir_bk/` | CommandA.sh 項番11（cp -r） | CommandA.sh 項番15（rmdir） |
| `/tmp/testdir/test.txt` | CommandA.sh 項番4（touch） | CommandA.sh 項番12（mv でリネーム） |
| `/tmp/testdir/data.txt` | CommandA.sh 項番5（echo） | CommandA.sh 項番17（rm -rf で間接削除） |
| `/tmp/testdir/data_bk.txt` | CommandA.sh 項番9（cp） | CommandA.sh 項番13（rm） |
| `/tmp/testdir/renamed.txt` | CommandA.sh 項番12（mv） | CommandA.sh 項番17（rm -rf で間接削除） |
