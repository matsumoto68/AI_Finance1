# ディレクトリ定義書

## リリース作業自動化 - シェルスクリプト

---

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | リリース作業自動化（Kiro導入） |
| ドキュメント種別 | ディレクトリ定義書 |
| 対象 | CommandA 自動化シェルスクリプト |
| 参照資料 | 外部設計書.md、内部設計書.md |

---

## 2. ディレクトリ構成

### 2.1 Linux環境（実行サーバ）

```
/
├── opt/
│   └── release/
│       └── scripts/                  # スクリプト配置ディレクトリ
│           ├── CommandA.sh           # メインシェルスクリプト
│           └── deploy_CommandA.sh    # 資材配置シェル
└── var/
    └── log/
        └── release/                  # ログ出力ディレクトリ（資材配置フォルダとは分離）
            ├── CommandA_YYYYMMDDHHMMSS.log         # シェル実行ログ
            └── CommandA_detail_YYYYMMDDHHMMSS.log  # 詳細ログ
└── tmp/
    ├── testdir/                      # 作業ディレクトリ（テスト用）
    │   ├── test.txt                  # ファイル作成（touch）対象
    │   ├── data.txt                  # データファイル
    │   ├── data_bk.txt               # データバックアップファイル
    │   └── renamed.txt               # リネーム後ファイル
    ├── testdir_bk/                   # バックアップディレクトリ（コピー先）
    └── testfile.sh                   # 権限変更対象ファイル
```

---

## 3. ディレクトリ定義

### 3.1 `/opt/release/scripts/`

| 項目 | 内容 |
|------|------|
| パス | `/opt/release/scripts/` |
| 用途 | シェルスクリプトの配置ディレクトリ |
| 作成タイミング | 資材配置シェル（`deploy_CommandA.sh`）実行時に存在しない場合自動作成 |
| 権限 | `755`（オーナー：読み書き実行、グループ/その他：読み取り・実行） |
| 備考 | 本番・試験環境共通パス |

### 3.2 `/var/log/release/`

| 項目 | 内容 |
|------|------|
| パス | `/var/log/release/` |
| 用途 | シェルスクリプトの実行ログ出力先（資材配置フォルダとは分離） |
| 作成タイミング | `CommandA.sh` の初期化処理（init_log）実行時に存在しない場合自動作成 |
| 権限 | `755` |
| 備考 | ログファイルは実行日時（YYYYMMDDHHMMSS）付きで生成 |

### 3.3 `/tmp/testdir/`

| 項目 | 内容 |
|------|------|
| パス | `/tmp/testdir/` |
| 用途 | ファイル・ディレクトリ操作コマンドの作業ディレクトリ |
| 作成タイミング | `CommandA.sh` のディレクトリ操作処理（項番1）で作成 |
| 権限 | `755` |
| 備考 | 処理の最後（項番17）で `rm -rf` により削除される |

### 3.4 `/tmp/testdir_bk/`

| 項目 | 内容 |
|------|------|
| パス | `/tmp/testdir_bk/` |
| 用途 | `testdir` のバックアップ（ディレクトリコピー先） |
| 作成タイミング | `CommandA.sh` のディレクトリコピー処理（項番11）で作成 |
| 権限 | コピー元と同じ `755` |
| 備考 | ディレクトリ削除処理（項番15）で削除される |

---

## 4. ファイル定義

### 4.1 シェルスクリプトファイル

| ファイル名 | 配置パス | 権限 | 用途 |
|-----------|---------|------|------|
| `CommandA.sh` | `/opt/release/scripts/CommandA.sh` | `755` | メインシェルスクリプト |
| `deploy_CommandA.sh` | `/opt/release/scripts/deploy_CommandA.sh` | `755` | 資材配置シェル |

### 4.2 ログファイル

| ファイル名 | 配置パス | 用途 | 命名規則 |
|-----------|---------|------|---------|
| シェル実行ログ | `/var/log/release/CommandA_YYYYMMDDHHMMSS.log` | INFO/ERRORメッセージ出力 | `CommandA_` + 実行日時（YYYYMMDDHHMMSS） + `.log` |
| 詳細ログ | `/var/log/release/CommandA_detail_YYYYMMDDHHMMSS.log` | コマンド・ReturnCode詳細記録 | `CommandA_detail_` + 実行日時（YYYYMMDDHHMMSS） + `.log` |

### 4.3 作業ファイル（テスト用）

| ファイル名 | 配置パス | 作成コマンド | 用途 |
|-----------|---------|------------|------|
| `test.txt` | `/tmp/testdir/test.txt` | `touch` | ファイル作成・移動テスト用 |
| `data.txt` | `/tmp/testdir/data.txt` | `echo hello >` | ファイル内容確認・コピーテスト用 |
| `data_bk.txt` | `/tmp/testdir/data_bk.txt` | `cp` | コピー先・削除テスト用 |
| `renamed.txt` | `/tmp/testdir/renamed.txt` | `mv` | リネーム後ファイル |
| `testfile.sh` | `/tmp/testfile.sh` | 事前配置 | 権限変更テスト用 |

---

## 5. 命名規則

### 5.1 シェルスクリプト

| 区分 | 規則 | 例 |
|------|------|----|
| メインシェル | `{コマンドID}.sh` | `CommandA.sh` |
| 資材配置シェル | `deploy_{コマンドID}.sh` | `deploy_CommandA.sh` |

### 5.2 ログファイル

| 区分 | 規則 | 例 |
|------|------|----|
| シェル実行ログ | `{コマンドID}_YYYYMMDDHHMMSS.log` | `CommandA_20260825143052.log` |
| 詳細ログ | `{コマンドID}_detail_YYYYMMDDHHMMSS.log` | `CommandA_detail_20260825143052.log` |

### 5.3 作業ディレクトリ・ファイル

| 区分 | 規則 |
|------|------|
| 作業ディレクトリ | `/tmp/` 配下に作成する |
| バックアップ | 元のディレクトリ名に `_bk` を付与 |
| バックアップファイル | 元のファイル名（拡張子なし）に `_bk` を付与 |

---

## 6. 権限定義

| 対象 | 権限値 | オーナー | グループ | その他 |
|------|--------|---------|---------|--------|
| シェルスクリプト（.sh） | `755` | rwx | r-x | r-x |
| ログディレクトリ | `755` | rwx | r-x | r-x |
| ログファイル（.log） | `644` | rw- | r-- | r-- |
| 作業ディレクトリ | `755` | rwx | r-x | r-x |

---

## 7. ディレクトリ作成・削除タイミング

| ディレクトリ/ファイル | 作成タイミング | 削除タイミング |
|---------------------|--------------|--------------|
| `/opt/release/scripts/` | `deploy_CommandA.sh` 実行時（存在しない場合に自動作成） | 手動削除（自動削除なし） |
| `/var/log/release/` | `CommandA.sh` 起動時 init_log 処理（存在しない場合に自動作成） | 手動削除（自動削除なし） |
| `/tmp/testdir/` | `CommandA.sh` ディレクトリ作成処理（`mkdir /tmp/testdir`） | `CommandA.sh` ディレクトリ強制削除処理（`rm -rf /tmp/testdir`） |
| `/tmp/testdir_bk/` | `CommandA.sh` ディレクトリコピー処理（`cp -r /tmp/testdir /tmp/testdir_bk`） | `CommandA.sh` ディレクトリ削除（空）処理（`rmdir /tmp/testdir_bk`） |
| `/tmp/testdir/test.txt` | `CommandA.sh` ファイル作成処理（`touch /tmp/testdir/test.txt`） | `CommandA.sh` ファイル移動（リネーム）処理（`mv` によりリネーム） |
| `/tmp/testdir/data.txt` | `CommandA.sh` ファイル作成処理（`echo hello > /tmp/testdir/data.txt`） | `CommandA.sh` ディレクトリ強制削除処理（`rm -rf` による間接削除）＋ `proc_file` 末尾の強制削除（`rm -rf /tmp/testdir`） |
| `/tmp/testdir/data_bk.txt` | `CommandA.sh` ファイルコピー処理（`cp /tmp/testdir/data.txt /tmp/testdir/data_bk.txt`） | `CommandA.sh` ファイル削除処理（`rm /tmp/testdir/data_bk.txt`） |
| `/tmp/testdir/renamed.txt` | `CommandA.sh` ファイル移動（リネーム）処理（`mv /tmp/testdir/test.txt /tmp/testdir/renamed.txt`） | `CommandA.sh` ディレクトリ強制削除処理（`rm -rf` による間接削除）＋ `proc_file` 末尾の強制削除（`rm -rf /tmp/testdir`） |
